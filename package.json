{
  "name": "web-push-notification-demo",
  "version": "1.0.0",
  "description": "An Example of using the Web Push API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "web-push": "^3.2.5"
  },
  "devDependencies": {
    "localtunnel": "^2.0.0"
  }
}
